<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 13+. The SDK requests it via requestNotificationPermission(activity);
         without the declaration the dialog never appears. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ExampleApp"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- Read by AppsOnAir Core. Replace with your own app ID or the SDK cannot
             register a subscription. -->
        <meta-data
            android:name="AppsonairAppId"
            android:value="YOUR_APP_ID" />

        <!-- AppsOnAir SDK: silhouette icon used by SDK-rendered (data-only) notifications.
             Android paints every non-transparent pixel white, so a full-colour icon renders
             as a white square. The recommended drawable name is ic_stat_appsonair_default. -->
        <meta-data
            android:name="com.appsonair.apppush.default_notification_icon"
            android:resource="@drawable/ic_stat_appsonair_default" />
        <meta-data
            android:name="com.appsonair.apppush.default_notification_color"
            android:resource="@color/appsonair_notification_accent_color" />

        <!-- Firebase: also declare Firebase keys so notification-block messages look identical
             to SDK-rendered ones. Point them at the same resources. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_appsonair_default" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/appsonair_notification_accent_color" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
